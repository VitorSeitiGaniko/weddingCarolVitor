<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

# Não reescreve se o arquivo ou diretório existir de fato

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
</IfModule>
